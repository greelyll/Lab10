class Sugar extends Cookie {
  private String cookieShape;
  private boolean cookieDecor;
  Sugar() {
    cookieShape = "";
    cookieDecor = false;

  }
  void setCookieShape() {
     cookieShape = shape;
   }

    double getCookieShape() {
     return cookieShape;
   }

   static void cut(String cookieShape, double cookieNumber) {

     System.out.println(number + "cookies were cut into" + cookieShape);
    getNumber () {
      return super.cookieNumber;
    }
   }
   
   static void decorate(boolean cookieDecor, boolean cookieBaked) {
     if (cookieBaked = true) {

   cookieDecor = true;
   }
   else { System.outprintln("Make sure to bake the cookies first.");} 

   }

   }
}